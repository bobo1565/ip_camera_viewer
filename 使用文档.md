# IP Camera Viewer 使用文档

## 📖 目录

1. [系统简介](#系统简介)
2. [系统要求](#系统要求)
3. [安装部署](#安装部署)
4. [功能说明](#功能说明)
5. [操作指南](#操作指南)
6. [常见问题](#常见问题)
7. [技术支持](#技术支持)

---

## 系统简介

IP Camera Viewer 是一个专业的局域网摄像头监控平台，支持低延迟的实时视频流播放。系统采用现代化的Web界面，提供直观的操作体验。

### 主要特性

- 🎥 支持多路RTSP视频流同时播放
- 🔍 自动扫描局域网内的ONVIF摄像头
- 📺 5画面布局（1个主窗口 + 4个副窗口）
- 🖱️ 拖拽式摄像头分配
- ⛶ 单窗口最大化查看
- 🔄 实时FPS显示和连接状态监控
- 💾 摄像头配置持久化存储

---

## 系统要求

### 服务器端

- **操作系统**: macOS / Linux / Windows
- **Python版本**: Python 3.7+
- **依赖库**:
  - Flask >= 2.3.0
  - OpenCV >= 4.8.0
  - onvif-zeep >= 0.2.12
  - flask-cors

### 客户端

- **浏览器**: Chrome 90+ / Firefox 88+ / Safari 14+ / Edge 90+
- **网络**: 与摄像头在同一局域网内

---

## 安装部署

### 1. 克隆或下载项目

```bash
cd /path/to/project
```

### 2. 创建虚拟环境（推荐）

```bash
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate  # Windows
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
```

### 4. 配置文件说明

编辑 `config.json` 文件：

```json
{
  "server": {
    "host": "0.0.0.0",      // 服务器监听地址
    "port": 5001,            // 服务器端口
    "debug": false           // 调试模式
  },
  "streaming": {
    "jpeg_quality": 70,      // JPEG压缩质量 (1-100)
    "max_fps": 25,           // 最大帧率
    "buffer_size": 3,        // 缓冲区大小
    "connection_timeout": 10 // 连接超时时间(秒)
  }
}
```

### 5. 启动服务

```bash
python main.py
```

### 6. 访问系统

打开浏览器访问：
- 本地访问: `http://localhost:5001`
- 局域网访问: `http://[服务器IP]:5001`

---

## 功能说明

### 界面布局

```
┌─────────────────────────────────────────────────────┐
│  🎥 IP Camera Viewer    在线:10  总计:10  [扫描][添加][刷新] │
├──────────┬──────────────────────────────┬──────────┤
│          │                              │  窗口1   │
│  摄像头  │                              ├──────────┤
│  列表    │        主窗口                │  窗口2   │
│          │                              ├──────────┤
│  (左侧)  │      (中间大窗口)            │  窗口3   │
│          │                              ├──────────┤
│          │                              │  窗口4   │
└──────────┴──────────────────────────────┴──────────┘
```

### 左侧摄像头列表

- 显示所有已添加的摄像头
- 绿点表示在线，红点表示离线
- 支持按IP或名称排序
- 可拖拽到视频窗口进行分配

### 视频窗口

- **主窗口**: 左侧大窗口，适合查看主要监控画面
- **副窗口**: 右侧4个小窗口，统一尺寸
- 每个窗口显示：
  - 窗口标题和摄像头名称
  - 实时视频流
  - 连接状态和FPS
  - 控制按钮（悬停显示）

### 控制按钮

鼠标悬停在视频窗口上时，右上角会显示三个按钮：

- **➕ 分配按钮**: 打开摄像头选择列表
- **⛶ 最大化按钮**: 全屏查看该窗口
- **✕ 清除按钮**: 移除该窗口的摄像头

---

## 操作指南

### 1. 扫描局域网摄像头

#### 步骤：
1. 点击顶部工具栏的 **[扫描网络]** 按钮
2. 系统会自动扫描局域网内的ONVIF摄像头
3. 扫描过程中显示进度条和发现数量
4. 扫描完成后显示发现的摄像头列表
5. 点击摄像头旁的 **[添加]** 按钮将其添加到系统

#### 注意事项：
- 扫描时间取决于网络规模，通常需要10-30秒
- 确保摄像头支持ONVIF协议
- 摄像头需要与服务器在同一局域网内

### 2. 手动添加摄像头

#### 步骤：
1. 点击顶部工具栏的 **[添加]** 按钮
2. 填写摄像头信息：
   - **名称**: 自定义摄像头名称（如：门口摄像头）
   - **RTSP地址**: 完整的RTSP流地址
   - **用户名**: 摄像头登录用户名（可选）
   - **密码**: 摄像头登录密码（可选）
3. 点击 **[添加]** 按钮保存

#### RTSP地址格式：
```
rtsp://[用户名]:[密码]@[IP地址]:[端口]/[路径]

示例：
rtsp://admin:admin123@192.168.1.100:554/stream1
rtsp://192.168.1.101:554/Streaming/Channels/101
```

### 3. 分配摄像头到窗口

#### 方法一：拖拽分配
1. 在左侧摄像头列表中找到目标摄像头
2. 按住鼠标左键拖拽摄像头
3. 拖到目标视频窗口上释放

#### 方法二：按钮分配
1. 鼠标悬停在目标视频窗口上
2. 点击右上角的 **➕** 按钮
3. 在弹出的列表中选择摄像头
4. 点击 **[选择]** 按钮

#### 方法三：右键菜单
1. 右键点击左侧摄像头列表中的摄像头
2. 选择"分配到主窗口"或"分配到窗口1-4"

### 4. 最大化查看

#### 步骤：
1. 鼠标悬停在要最大化的视频窗口上
2. 点击右上角的 **⛶** 按钮
3. 窗口会全屏显示，隐藏其他窗口和侧边栏
4. 再次点击 **⛶** 按钮或按 **ESC** 键退出全屏

### 5. 清除窗口

#### 步骤：
1. 鼠标悬停在要清除的视频窗口上
2. 点击右上角的 **✕** 按钮
3. 该窗口会清空，可以重新分配其他摄像头

### 6. 移除摄像头

#### 步骤：
1. 右键点击左侧摄像头列表中的摄像头
2. 选择"移除摄像头"
3. 确认删除
4. 摄像头会从系统中移除，相关窗口会自动清空

### 7. 排序摄像头列表

#### 步骤：
1. 在左侧摄像头列表顶部找到排序按钮
2. 点击 **[IP]** 按钮：按IP地址排序
3. 点击 **[名称]** 按钮：按摄像头名称排序

---

## 常见问题

### Q1: 扫描不到摄像头怎么办？

**A**: 请检查以下几点：
1. 摄像头是否支持ONVIF协议
2. 摄像头和服务器是否在同一局域网内
3. 摄像头的ONVIF功能是否已启用
4. 防火墙是否阻止了扫描端口（80, 8080, 2020, 8000）
5. 尝试手动添加摄像头

### Q2: 视频流无法播放？

**A**: 可能的原因和解决方法：
1. **RTSP地址错误**: 检查RTSP地址格式是否正确
2. **认证失败**: 确认用户名和密码正确
3. **网络问题**: 检查网络连接，尝试ping摄像头IP
4. **端口被占用**: 确认RTSP端口（通常554）未被防火墙阻止
5. **编码格式不支持**: 摄像头使用的编码格式可能不被OpenCV支持

### Q3: 视频延迟很高怎么办？

**A**: 优化建议：
1. 降低 `config.json` 中的 `jpeg_quality` 值（如改为50）
2. 减少 `buffer_size` 值（如改为1）
3. 确保服务器和摄像头之间网络畅通
4. 减少同时播放的视频流数量
5. 使用有线网络代替WiFi

### Q4: 浏览器显示"连接失败"？

**A**: 检查步骤：
1. 确认服务器正在运行（查看终端输出）
2. 检查浏览器访问的地址和端口是否正确
3. 查看浏览器控制台是否有错误信息
4. 尝试清除浏览器缓存后重新访问
5. 检查防火墙是否阻止了5001端口

### Q5: 摄像头显示"离线"但实际在线？

**A**: 可能原因：
1. 摄像头重启或网络临时中断
2. RTSP流超时
3. 点击 **[刷新]** 按钮重新加载摄像头列表
4. 尝试清除该窗口后重新分配

### Q6: 如何修改服务器端口？

**A**: 
1. 编辑 `config.json` 文件
2. 修改 `server.port` 的值
3. 重启服务器
4. 使用新端口访问系统

### Q7: 数据会保存吗？

**A**: 
- 添加的摄像头信息会自动保存到 `cameras_db.json` 文件
- 窗口分配关系不会保存，每次刷新页面需要重新分配
- 如需保存窗口布局，可以考虑使用浏览器书签功能

### Q8: 支持多少路视频同时播放？

**A**: 
- 理论上支持任意数量，但受限于：
  - 服务器性能（CPU、内存、网络带宽）
  - 浏览器性能
  - 网络带宽
- 建议同时播放不超过10路视频流
- 当前界面最多显示5路（1主+4副）

---

## 技术支持

### 日志查看

服务器运行日志会在终端实时显示，包括：
- 摄像头连接状态
- 视频流FPS
- HTTP请求记录
- 错误信息

### 配置文件位置

- **服务器配置**: `config.json`
- **摄像头数据库**: `cameras_db.json`
- **Web界面**: `web/templates/index.html`
- **样式文件**: `web/static/css/style.css`
- **脚本文件**: `web/static/js/app.js`

### 系统架构

```
┌─────────────┐
│   浏览器    │ ← HTTP/MJPEG
└──────┬──────┘
       │
┌──────▼──────┐
│ Flask服务器 │
└──────┬──────┘
       │
┌──────▼──────┐
│  OpenCV     │ ← RTSP
└──────┬──────┘
       │
┌──────▼──────┐
│  IP摄像头   │
└─────────────┘
```

### 性能优化建议

1. **服务器端**:
   - 使用SSD存储
   - 确保足够的内存（建议4GB+）
   - 使用有线网络连接

2. **网络优化**:
   - 使用千兆交换机
   - 摄像头和服务器在同一网段
   - 避免WiFi连接摄像头

3. **浏览器优化**:
   - 使用Chrome或Edge浏览器
   - 关闭不必要的浏览器扩展
   - 定期清理浏览器缓存

### 安全建议

1. **网络安全**:
   - 不要将服务器暴露到公网
   - 使用防火墙限制访问
   - 定期更新摄像头固件

2. **认证安全**:
   - 使用强密码
   - 定期更换摄像头密码
   - 不要在配置文件中明文存储密码

3. **访问控制**:
   - 限制局域网内访问
   - 考虑添加Web认证
   - 使用HTTPS（需要额外配置）

---

## 版本信息

- **当前版本**: 1.0.0
- **最后更新**: 2026-02-25
- **Python版本**: 3.7+
- **Flask版本**: 2.3.0+

---

## 许可证

本项目仅供学习和个人使用。

---

## 联系方式

如有问题或建议，请通过以下方式联系：
- 项目地址: [GitHub仓库地址]
- 问题反馈: [Issues页面]

---

**祝您使用愉快！** 🎉
